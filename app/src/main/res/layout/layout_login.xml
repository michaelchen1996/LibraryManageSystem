<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="cn.edu.scu.zy.librarymanagesystem.MainActivity">

    <ImageView
        android:id="@+id/image_loginCenter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_launcher"

        android:layout_marginTop="81dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:id="@+id/edit_id"
        android:layout_width="match_parent"
        android:layout_height="50sp"
        android:inputType="number"
        android:hint="Reader/Admin ID"
        android:textSize="24sp"
        android:layout_below="@id/image_loginCenter" />

    <EditText
        android:id="@+id/edit_password"
        android:layout_width="match_parent"
        android:layout_height="50sp"
        android:inputType="textPassword"
        android:hint="Password"
        android:textSize="24sp"
        android:layout_below="@+id/edit_id"
        android:layout_alignStart="@+id/edit_id" />

    <Button
        android:id="@id/button_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Login"
        android:layout_below="@+id/button_visitor"
        android:layout_alignEnd="@+id/button_visitor" />

    <Button
        android:id="@+id/button_visitor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="visitor"
        android:layout_below="@+id/edit_password"
        android:layout_alignParentStart="true" />


</RelativeLayout>
